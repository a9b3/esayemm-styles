{
  "name": "esayemm-styles",
  "version": "3.0.0",
  "description": "esayemm's shared styles",
  "main": "./global.css",
  "scripts": {
    "build": "node ./scripts/generate-output.js",
    "preversion": "npm run build",
    "version": "git add .",
    "postversion": "git push && git push --tags && npm publish ./"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/esayemm/esayemm-styles.git"
  },
  "author": "Sam",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/esayemm/esayemm-styles/issues"
  },
  "homepage": "https://github.com/esayemm/esayemm-styles#readme",
  "dependencies": {
    "normalize.css": "^6.0.0"
  }
}
